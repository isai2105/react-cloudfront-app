# Enforcement half of the Node pin: `.nvmrc` picks the version, `engines` in
# package.json declares it, and this flag makes pnpm refuse to install on
# anything older instead of warning.
engine-strict=true
